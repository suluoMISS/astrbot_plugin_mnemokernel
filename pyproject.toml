[project]
name = "astrbot-plugin-mnemokernel"
version = "0.1.0"
description = "Evidence-grounded, on-demand long-term memory kernel for AstrBot"
license = "AGPL-3.0-or-later"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
