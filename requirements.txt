# AstrBot installs plugin dependencies from this file for source/GitHub installs.
# The source tree carries both wheels locally, so plugin installation does not
# need to download a binary from GitHub before the plugin can start.
./native/mnemokernel_native-0.1.1-cp310-abi3-manylinux_2_34_x86_64.whl ; sys_platform == "linux" and platform_machine == "x86_64"
./native/mnemokernel_native-0.1.1-cp310-abi3-win_amd64.whl ; sys_platform == "win32" and platform_machine == "AMD64"
